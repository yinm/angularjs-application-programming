describe('Jasmineの基本', function() {
  beforeEach(function(){
  });

  it('基本テスト', function() {
    expect(1 + 1).toEqual(2);
    //expect(1 + 1).not.toEqual(2);
    //expect(1 + 1).toEqual(3);
  });
});
